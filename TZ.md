# Техническое задание: Арбитражный веб-терминал

## Введение

Данный документ описывает требования к разработке автоматизированного арбитражного торгового терминала с веб-интерфейсом. Система предназначена для хеджированной торговли фьючерсами между несколькими криптовалютными биржами в режиме реального времени.

### Основная концепция

Терминал автоматически отслеживает ценовые расхождения (спреды) по выбранным пользователем торговым парам на разных биржах и совершает одновременные сделки (лонг/шорт) для извлечения прибыли из разницы цен. Пользователь взаимодействует с ботом через веб-интерфейс (браузер), без необходимости программировать или использовать командную строку.

### Цель проекта

Создать полностью автоматизированный арбитражный торговый терминал (бот) для торговли фьючерсами между криптобиржами:

- Пользователь через веб-интерфейс подключает свои биржевые аккаунты
- Задаёт параметры арбитражной стратегии (торговые пары, объём позиции, пороги спреда входа/выхода, стоп-лосс и т.д.)
- Запускает бота
- Бот самостоятельно мониторит цены на подключенных биржах, учитывает комиссии и ограничения
- При благоприятных условиях автоматически открывает парные позиции: длинную позицию на бирже с более дешёвой ценой актива и короткую позицию на бирже с более дорогой ценой
- После открытия бот сопровождает позиции и автоматически закрывает обе при выполнении заданных условий

---

## Архитектура системы

Терминал построен по принципу **клиент-сервер**:

### Серверная часть (Backend)
- Бот, работающий на VPS 24/7
- Через API бирж собирает рыночные данные (цены, стаканы ордеров)
- Анализирует спреды и принимает решения об открытии/закрытии позиций
- Управление рисками (учёт маржи, комиссий, стоп-лоссов)
- Обработка ошибок (сбой API, частичное исполнение, ликвидации)

### Клиентская часть (Frontend)
- Веб-приложение (браузерный интерфейс)
- Подключение бирж
- Добавление торговых пар
- Управление ботом (старт/пауза пар)
- Отображение статистики и уведомлений

### Обмен данными
- Защищённый канал (HTTPS, WebSocket)
- Обновление в реальном времени для цен и состояний сделок

---

## Поддерживаемые биржи

На первом этапе терминал работает со следующими криптовалютными биржами (фьючерсные платформы):

| Биржа | Статус |
|-------|--------|
| Bybit | Поддерживается |
| Bitget | Поддерживается |
| BingX | Поддерживается |
| Gate.io | Поддерживается |
| OKX | Поддерживается |
| HTX | Поддерживается |
| MEXC | Поддерживается |

**Важно:**
- Для работы арбитража необходимо минимум **две подключенные биржи**
- Терминал рассчитан на **одного пользователя** (один набор API-ключей для каждой биржи)
- Много-пользовательский режим не предусмотрен в первой версии

---

## Интерфейс пользователя

Веб-интерфейс состоит из трёх основных вкладок:

### Вкладка «БОТ»

Основной экран для добавления и отслеживания торговых пар.

#### 1. Информация о балансах и парах

В верхней части вкладки отображается сводная информация:

```
Bitget: 1500 USDT | MEXC: 1200 USDT | ...
```

- Указываются фьючерсные балансы (маржинальные средства)
- Баланс показан как equity: свободная маржа + текущий PNL по позициям
- Обновление балансов: **каждую минуту**
- Отображается количество добавленных/запущенных пар

#### 2. Добавление новой пары

Кнопка «Добавить пару» по центру:
- Неактивна при достижении лимита **30 пар**
- Открывает всплывающую форму с полями:

| Поле | Описание |
|------|----------|
| **Торговая пара (актив)** | Выбор актива, например BTC/USDT. Автозаполнение на основе доступных фьючерсных контрактов. Актив должен присутствовать минимум на двух подключенных биржах |
| **Рабочий объём** | Величина позиции в монетах актива (например, 0.5 BTC). Рядом отображается эквивалент в USDT |
| **Количество ордеров** | На сколько частей делить рабочий объём при входе (для снижения проскальзывания) |
| **Спред входа (%)** | Минимальное расхождение цен для входа в сделку |
| **Спред выхода (%)** | Целевой уровень спреда для закрытия с прибылью |
| **Stop Loss (USDT)** | Допустимый убыток по связке (опционально) |

Кнопки: «Добавить» и «Отмена»
- Новая пара добавляется в состоянии **паузы**

#### 3. Список торговых пар (карточки)

Каждая пара отображается в виде карточки:
- Сетка: 3 в ряд на широком экране, адаптивно на мобильных

**Цветовая индикация статуса:**

| Цвет рамки | Статус |
|------------|--------|
| 🟢 Зелёный | Активный арбитраж (открыты позиции) |
| 🟠 Оранжевый | Пара на паузе |
| ⚪ Белый | Запущена, ожидает условий входа |

**Содержимое карточки:**

```
┌─────────────────────────────────────┐
│ BTC/USDT                            │
│ Р.О: 0.5 BTC С.В: 1.0% С.ВЫ: 0.2%  │
│ N.ОР: 4 SL: 100                     │
├─────────────────────────────────────┤
│ [Торговая информация / Статус]      │
├─────────────────────────────────────┤
│ N сделок: 5, PNL: +45 USDT          │
├─────────────────────────────────────┤
│ [▶ Старт] [✎ Изменить] [🗑 Удалить] │
└─────────────────────────────────────┘
```

**При активном арбитраже отображается:**
- Названия бирж с типом позиции: `Binance (Long)` / `Gate (Short)`
- Текущая цена актива на каждой бирже
- Нереализованный PNL по каждой ноге
- Суммарный PNL по связке
- Обновление: **раз в секунду**

**Кнопки управления:**
- **Старт/Стоп (Пауза)**: переключение мониторинга
- **Изменить (✎)**: редактирование параметров (вступает в силу после закрытия текущей позиции)
- **Удалить (🗑)**: удаление пары (только без открытой позиции, требует подтверждения)

#### 4. Чёрный список пар

Информативный раздел для пометки нежелательных активов:
- Ручное добавление/удаление
- Носит справочный характер (бот не фильтрует автоматически)

---

### Вкладка «API/STATS»

#### Секция API (подключение бирж)

Список поддерживаемых бирж с:
- Логотипом/названием
- Статусом: 🟢 «Подключено» / 🔴 «Не подключено»
- Полями для API Key и Secret Key (если не подключено)
- Кнопками: «Подключить» / «Изменить» / «Отключить»
- Балансом маржи: `Баланс: 1200 USDT`

**Обновление балансов:** каждую минуту + кнопка «Обновить»

**Важно:** Без минимум 2 подключенных бирж арбитраж невозможен

#### Секция STATS (статистика)

| Метрика | Периоды | Действия |
|---------|---------|----------|
| Количество завершённых арбитражей | День/Неделя/Месяц | Кнопка «Обнулить» |
| Общий PNL | День/Неделя/Месяц | Кнопка «Обнулить» |
| Срабатывания Stop Loss | День/Неделя/Месяц | Список деталей, кнопка «Очистить» |
| Количество ликвидаций | День/Неделя/Месяц | Список деталей, кнопка «Очистить» |

**Топ-5 списки:**
- По количеству арбитражей
- Убыточные пары
- Прибыльные пары

---

### Вкладка «Уведомления»

#### Лента уведомлений

Хронологический список событий (новые сверху):

```
[12:05:34] ✅ Открыт арбитраж по паре Bitget/OKX BTCUSDT (лонг 25000, шорт 25050, спред 0.2%)
[12:15:10] ⚠ Не удалось открыть позицию на MEXC по паре ETHUSDT (вторая нога). Сделка отменена.
[12:45:00] ⏸ Пара BTCUSDT поставлена на паузу (Stop Loss сработал, -$30).
```

**Цветовая индикация:**
- ✅ Зелёный — успешные операции
- ⚠ Жёлтый — предупреждения
- ❌ Красный — ошибки

#### Фильтры типов событий (чекбоксы)

- [ ] Открытие сделки
- [ ] Закрытие сделки
- [ ] Stop Loss
- [ ] Ликвидация
- [ ] Ошибка API/ордера
- [ ] Недостаток маржи
- [ ] Пауза/Остановка
- [ ] Вторая нога не открыта

Кнопка «Очистить журнал»

---

## Логика арбитражной торговли

### 1. Мониторинг цен и расчёт спреда

**Получение данных:**
- WebSocket-соединения бирж (где доступно)
- Регулярный опрос REST API (как fallback)
- Частота: **ежесекундно** или максимально возможная

**Расчёт спреда:**

```
Спред (%) = ((Цена_дорогая - Цена_дешевая) / Цена_дешевая) × 100%
```

**Учёт комиссий:**

```
Net_spread (%) = Спред (%) - Суммарные_комиссии (%)
Суммарные_комиссии = 2 × Комиссия_биржи_A + 2 × Комиссия_биржи_B
```

**Проверка глубины рынка:**
- Анализ стакана ордеров (Level2)
- Расчёт средневзвешенной цены для требуемого объёма
- Дешёвая биржа: пройти по аскам
- Дорогая биржа: пройти по бидам

**Условие входа:**
```
net_spread >= порог_входа AND достаточно_маржи AND ликвидность_достаточна
```

### 2. Вход в позицию

#### Проверки перед входом:

1. **Маржа:** достаточно средств на обеих биржах
2. **Лимиты позиций:** не превышены максимальные размеры
3. **Мин/макс ордера:** объём в допустимых пределах
4. **Равенство объёмов:** обязательно одинаковый объём на обеих биржах

#### Исполнение ордеров:

- **Тип ордеров:** рыночные (market)
- **Синхронизация:** два ордера отправляются параллельно (асинхронно)
- **Разрыв:** миллисекунды

#### Алгоритм частичного входа (N > 1):

```
1. Разбить объём на N равных частей
2. Для каждой части i от 1 до N:
   a. Пересчитать текущий спред
   b. Если net_spread >= порог_входа:
      - Отправить рыночные ордера на обе биржи
      - Дождаться исполнения
   c. Если net_spread < порог_входа:
      - Приостановить вход
      - Ожидать расширения спреда
   d. Если net_spread <= порог_выхода:
      - Перейти к закрытию уже открытой части
```

#### Обработка ошибок при открытии:

**Сценарий: открылась только одна позиция**

```
1. Повторные попытки открытия второй ноги (высокая частота)
2. Если успех → продолжить нормальную работу
3. Если спред исчез или постоянные ошибки:
   a. Закрыть первую позицию по рынку
   b. Поставить пару на паузу
   c. Отправить уведомление об ошибке
```

### 3. Сопровождение и закрытие позиций

#### Условия выхода:

| Условие | Действие |
|---------|----------|
| **Спред <= спред_выхода** | Закрытие с прибылью |
| **Суммарный PNL <= -SL** | Закрытие по стоп-лоссу |
| **Ликвидация одной позиции** | Экстренное закрытие второй |

#### Закрытие по целевому спреду:

```
1. Обнаружено: спред <= спред_выхода
2. Если позиция открывалась N частями:
   - Закрывать по частям (аналогично входу)
   - Для каждой части проверять спред <= спред_выхода
3. Отправить рыночные ордера на закрытие обеих позиций
4. Обновить статистику (+1 сделка, +PNL)
5. Пара возвращается в режим мониторинга
```

#### Закрытие по Stop Loss:

```
1. Мониторинг: PNL_total = PNL_long + PNL_short
2. Если PNL_total <= -SL:
   a. Немедленно закрыть обе позиции по рынку
   b. Поставить пару на паузу
   c. Отправить уведомление
   d. Обновить статистику SL
```

#### Закрытие при ликвидации:

```
1. Получено событие ликвидации от биржи
2. Немедленно закрыть оставшуюся позицию
3. Поставить пару на паузу
4. Отправить уведомление
5. Обновить статистику ликвидаций
```

### 4. Машина состояний для пары

```
PAUSED (пауза)
   │
   └──▶ READY (мониторинг)
           │
           ├──▶ ENTERING_PART_i (вход, часть i)
           │         │
           │         ├─ успех → NEXT_PART или HOLD_FOR_SPREAD
           │         │
           │         └─ сбой второй ноги → RETRY или CANCEL → PAUSED
           │
           └──▶ HOLD_FOR_SPREAD (ожидание выхода)
                     │
                     ├─ спред выхода достигнут → CLOSE_BOTH → READY
                     │
                     ├─ SL достигнут → CLOSE_BOTH → PAUSED
                     │
                     └─ ликвидация → CLOSE_OTHER → PAUSED
```

---

## REST API и WebSocket

### Управление биржами

| Метод | Эндпойнт | Описание |
|-------|----------|----------|
| POST | `/api/exchanges/{name}/connect` | Подключение биржи |
| DELETE | `/api/exchanges/{name}/connect` | Отключение биржи |
| GET | `/api/exchanges` | Список бирж и их статусов |

**Формат ответа GET /api/exchanges:**
```json
[
  {
    "name": "Bitget",
    "connected": true,
    "balance": {
      "total": 1500,
      "currency": "USDT"
    }
  }
]
```

### Управление торговыми парами

| Метод | Эндпойнт | Описание |
|-------|----------|----------|
| POST | `/api/pairs` | Добавление новой пары |
| GET | `/api/pairs` | Список всех пар |
| PATCH | `/api/pairs/{id}` | Редактирование пары |
| POST | `/api/pairs/{id}/start` | Запуск мониторинга |
| POST | `/api/pairs/{id}/pause` | Приостановка |
| DELETE | `/api/pairs/{id}` | Удаление пары |

**Формат объекта пары:**
```json
{
  "id": 1,
  "symbol": "BTCUSDT",
  "entrySpread": 1.0,
  "exitSpread": 0.2,
  "volume": 0.5,
  "nOrders": 4,
  "stopLoss": 100,
  "status": "paused",
  "stats": {
    "tradesCount": 5,
    "totalPnl": 45.0
  }
}
```

### Уведомления и статистика

| Метод | Эндпойнт | Описание |
|-------|----------|----------|
| GET | `/api/notifications?types=A,B,C` | Список уведомлений с фильтрацией |
| GET | `/api/stats` | Агрегированная статистика |

### WebSocket

**Эндпойнт:** `WS /ws/stream`

**Типы сообщений:**

```json
// Обновление пары
{
  "type": "pairUpdate",
  "id": 1,
  "status": "active",
  "currentSpread": 0.8,
  "pnl": 5.0
}

// Уведомление
{
  "type": "notification",
  "notif": {
    "timestamp": "...",
    "type": "SL",
    "message": "Stop Loss triggered..."
  }
}

// Обновление баланса
{
  "type": "balanceUpdate",
  "exchange": "Bitget",
  "balance": 1600.0
}

// Обновление статистики
{
  "type": "statsUpdate",
  "totalTrades": 100,
  "totalPnl": 500.0
}
```

---

## Модель данных

### ExchangeAccount

```typescript
interface ExchangeAccount {
  id: number;
  name: string;           // "bitget", "mexc", etc.
  apiKey: string;         // Зашифровано
  apiSecret: string;      // Зашифровано
  connected: boolean;
  lastError?: string;
  balances: {
    USDT: number;
  };
  updatedAt: Date;
}
```

### PairConfig

```typescript
interface PairConfig {
  id: number;
  symbol: string;         // "BTCUSDT"
  base: string;           // "BTC"
  quote: string;          // "USDT"
  entrySpreadPct: number; // 1.0
  exitSpreadPct: number;  // 0.2
  volumeAsset: number;    // 0.5
  nOrders: number;        // 4
  slLimit?: number;       // 100 (USDT)
  status: 'paused' | 'active';
}
```

### PairRuntime

```typescript
interface PairRuntime {
  pairId: number;
  state: 'PAUSED' | 'READY' | 'ENTERING' | 'HOLD' | 'CLOSING';
  currentLegs?: [
    {
      exchange: string;
      side: 'long' | 'short';
      entryPrice: number;
      currentPrice: number;
      qty: number;
      pnlUnreal: number;
    },
    {
      exchange: string;
      side: 'long' | 'short';
      entryPrice: number;
      currentPrice: number;
      qty: number;
      pnlUnreal: number;
    }
  ];
  filledParts: number;
  lastSpreadPct: number;
  pnlTotalUnreal: number;
  pnlTotalRealized: number;
  lastEventTime: Date;
}
```

### Notification

```typescript
interface Notification {
  id: number;
  timestamp: Date;
  type: 'OPEN' | 'CLOSE' | 'SL' | 'LIQUIDATION' | 'ERROR' | 'MARGIN' | 'PAUSE' | 'SECOND_LEG_FAIL';
  severity: 'info' | 'warn' | 'error';
  pairId?: number;
  message: string;
  meta?: object;
}
```

### Settings

```typescript
interface Settings {
  considerFunding: boolean;           // false по умолчанию
  maxConcurrentTrades?: number;       // null = без ограничений
  notificationPrefs: {
    open: boolean;
    close: boolean;
    sl: boolean;
    liq: boolean;
    apiError: boolean;
    margin: boolean;
    pause: boolean;
    secondLegFail: boolean;
  };
}
```

---

## Формулы расчёта

### Расчёт спреда с учётом объёма и комиссий

```
1. На дешёвой бирже A: 
   - Пройти по аскам, суммировать до объёма V
   - Получить средневзвешенную цену P_buy_A

2. На дорогой бирже B:
   - Пройти по бидам, суммировать до объёма V
   - Получить средневзвешенную цену P_sell_B

3. Спред до комиссии:
   spread% = ((P_sell_B - P_buy_A) / P_buy_A) × 100%

4. Учёт комиссий:
   fee_sum% = 2 × f_A + 2 × f_B  (вход + выход на каждой бирже)

5. Чистый спред:
   net_spread% = spread% - fee_sum%
```

### Расчёт PNL

```
Q = количество актива (одинаково в обеих позициях)

PNL_long = (P_long_out - P_long_in) × Q
PNL_short = (P_short_in - P_short_out) × Q

PNL_total = PNL_long + PNL_short

Нереализованный PNL:
PNL_long_unreal = (P_current_A - P_long_in) × Q
PNL_short_unreal = (P_short_in - P_current_B) × Q
```

---

## Нефункциональные требования

### Производительность

| Параметр | Требование |
|----------|------------|
| Задержка обнаружение → ордер | < 1 секунда |
| Обновление UI (цены, PNL) | 1 раз в секунду |
| Обновление балансов | 1 раз в минуту |
| Rate limits | Не превышать лимиты API бирж |

### Надёжность

- Работа 24/7 без утечек памяти и зависаний
- Восстановление после перезапуска:
  - Автоматическое подключение к биржам
  - Восстановление мониторинга активных пар
  - Проверка открытых позиций через API бирж
- Логирование всех ключевых событий
- Тайм-ауты и обработка исключений

### Безопасность

| Требование | Реализация |
|------------|------------|
| Авторизация | Логин/пароль для доступа к интерфейсу |
| Шифрование данных | API-ключи хранятся зашифрованными |
| HTTPS | Обязательно для внешнего доступа |
| Валидация ввода | Проверка всех входящих данных |
| 2FA | Опционально (желательно в будущем) |

### Масштабируемость

- **Модульная архитектура:** унифицированный интерфейс для биржевых API
- **Добавление бирж:** без изменения ядра системы
- **Нагрузка:** до 30 пар × 6 бирж = 180 потоков цен
- **Оптимизация:** асинхронное программирование, event loop

---

## Технический стек (рекомендации)

### Backend
- Node.js / Python (asyncio)
- WebSocket библиотеки для бирж
- База данных: PostgreSQL / MongoDB
- Redis для кеширования

### Frontend
- React / Vue.js
- WebSocket клиент
- Responsive design (адаптивный)

### Инфраструктура
- VPS (Linux)
- Docker (опционально)
- Nginx (reverse proxy, SSL)

---

## Краткая сводка требований

### Обязательные функции (MVP)

- [ ] Подключение минимум 2 бирж через API
- [ ] Добавление до 30 торговых пар
- [ ] Мониторинг цен в реальном времени
- [ ] Расчёт спреда с учётом комиссий и глубины рынка
- [ ] Автоматическое открытие арбитражных позиций
- [ ] Частичный вход (N ордеров)
- [ ] Закрытие по целевому спреду
- [ ] Stop Loss по общему PNL
- [ ] Обработка ликвидаций
- [ ] Обработка ошибок (неоткрытая вторая нога)
- [ ] Вкладка БОТ с карточками пар
- [ ] Вкладка API/STATS
- [ ] Вкладка Уведомления
- [ ] REST API для управления
- [ ] WebSocket для обновлений в реальном времени

### Дополнительные функции (Nice to have)

- [ ] 2FA авторизация
- [ ] Telegram уведомления
- [ ] Экспорт статистики
- [ ] Бэктестинг стратегий
- [ ] Много-пользовательский режим

---

*Документ создан для использования с Claude Code. Версия 1.0*
